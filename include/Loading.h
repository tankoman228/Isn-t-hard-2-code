#pragma once

#include <UI.h>
#include <Particles.h>
#include <Voids.h>

void loadTexture(const std::string key, const std::string& path);
Texture* getTexture(const std::string& path);
void loading();
